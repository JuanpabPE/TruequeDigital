# Copia este archivo a .env y llena los valores

# MongoDB URI (local o MongoDB Atlas)
MONGODB_URI=mongodb://localhost/merndb

# Token secret (genera uno seguro con: openssl rand -base64 64)
TOKEN_SECRET=your_super_secret_token_key_change_this_in_production

# Puerto del servidor
PORT=3000

# URL del frontend (cambia en producción)
FRONTEND_URL=http://localhost:5173

# Entorno (development o production)
NODE_ENV=development
